$('#participation').html("<%= j(render partial: 'participations/go', locals: {event: @participation.event}) %>");
<% flash.each do |type, message| %>
  $("#flash").html('<div class="<%= type %>"><div class="msg"><%= message %></div></div>');
<% end %>
