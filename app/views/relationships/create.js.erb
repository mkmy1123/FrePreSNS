$("#trust-ajax").html("<%= j(render("relationships/untrust_btn", { user: @user })) %>");
$("#trusted").html("<%= j(render("relationships/trusted_count", { user: @user })) %>");
<% flash.each do |type, message| %>
  $("#flash").html('<div class="<%= type %>"><div class="msg"><%= message %></div></div>');
<% end %>